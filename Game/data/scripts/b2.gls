
pi := 3.14159;
tau := 2 * pi;

platform_range :: Vector2.{ 0, 0.5 }?;

foreach(moving_1, moving_2) {
    cycle := cycle_over_random(time, 5, 8);
    set_next_offset(it, circle(cycle, 0) * platform_range);
}
